---
description: 用户指定的软件开发工作原则记忆
alwaysApply: true
---

# 软件开发工作原则（用户记忆）

1. 不采用“绝对不改代码和文档”的规则；在充分理解后允许并应当进行必要改动。
2. 开始工作前，必须先阅读并理解当前代码、文档与历史变更（尤其是需求、设计、测试与 Change Log）。
3. 新功能必须补充对应测试，并并入现有测试框架与测试文件体系。
4. 任何改动都要设计并执行相应测试；测试通过后才可交付。
5. 任何代码改动都必须同步更新文档：`DESIGN.md`、`README.md`；变更记录写入 `DESIGN.md` 的 `Change Log`。
6. 需要读取 PDF 时，参考 `/Users/wukun/Documents/jl/AGENTS.md` 中的本地读取方式。
7. 清理临时产物：调试临时文件、临时安装工具、临时测试代码等在完成后必须移除，避免留下垃圾文件。
