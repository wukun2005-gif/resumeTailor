# Gemini API Key — 仅用于 E2E 测试（test-e2e.mjs）
# 服务端的 API Key 通过前端设置界面配置，不依赖此文件
GEMINI_KEY=your_gemini_api_key_here

# 以下为可选配置，有默认值
# PORT=3001
# NODE_ENV=development
# GEMINI_FALLBACK_MODELS=gemini-2.5-flash,gemini-2.0-flash
